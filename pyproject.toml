[project]
name = "apt29-elastic-lab"
version = "1.0.0"
description = "APT29 ATT&CK Evals Day 1 loaded into a local Elasticsearch + Kibana lab"
requires-python = ">=3.11"
dependencies = ["requests>=2.32"]
