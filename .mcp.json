{
  "mcpServers": {
    "elasticsearch": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "ES_URL=http://host.docker.internal:9280",
        "docker.elastic.co/mcp/elasticsearch", "stdio"
      ]
    }
  }
}
